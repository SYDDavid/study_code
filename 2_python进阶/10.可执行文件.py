# 步骤一：pip install pyinstaller
# 步骤二：在对应py文件目录下，启动cmd窗口
# 步骤三：pyinstaller byhy.py --workpath d:\pybuild  --distpath d:\pybuild\dist

## 结果文件存放在：d:\pybuild\dist

'''
    --workpath      配置工作目录
    --distpath      配置结果目录
    --noconsole     不显示黑色dos界面
    --onefile       结果仅为一个可执行文件
'''